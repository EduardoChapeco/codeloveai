---
status: done
priority: high
created: 2026-02-24
---

## Tarefa: Plano de implementação e correções

### Descrição
Implement and verify all Starble v2.0 system requirements.

### Status: DONE

### Verified Implementation:

#### Database (All tables exist with correct columns):
- ✅ `licenses` — has plan_id, status, type, trial fields, token_valid_until, usage counters, device_id
- ✅ `plans` — fully configurable per tenant, with type/billing_cycle/modules/extension_mode
- ✅ `tenant_branding` — extension_mode, custom_mode_prompt, modules, community settings, trial_minutes
- ✅ `tenants` — owner_user_id, affiliate_id, platform_fee_per_user, setup_paid_at
- ✅ `affiliates` — type (simple/whitelabel), pix_key, bank_info
- ✅ `commissions` — affiliate_id, tenant_id, license_id, type, status, payout_batch_id
- ✅ `payout_batches` — status, total_amount, processed_at
- ✅ `community_channels` — tenant_id (null=global), is_private, is_readonly
- ✅ `community_messages` — NO tenant_id (by design)
- ✅ `community_profiles` — NO tenant_id (by design)
- ✅ `daily_usage` — license_id, messages_used, date
- ✅ `rate_limits` — key, count, window_start

#### RLS Policies (All critical policies in place):
- ✅ Community channels: users see global OR own tenant channels
- ✅ Community messages: users see messages from accessible channels only
- ✅ Community profiles: no tenant leakage
- ✅ Plans: public view for active plans, tenant admin management
- ✅ Licenses: user owns own, admin manages all
- ✅ Commissions: admin + affiliate view

#### Edge Functions (All deployed):
- ✅ `validate-license` — license key + device_id + trial/token/daily limits
- ✅ `validate-hwid` — device_id binding (correct column name)
- ✅ `get-user-context` — full branding/plan/modules/links response
- ✅ `consume-token` — increment counters, check limits
- ✅ `renew-token` — 24h token renewal
- ✅ `start-trial` — one-time 30min trial per user_id
- ✅ `increment_daily_usage` RPC function

#### Fixes Applied:
- ✅ `tenant-resolver.ts` — added `id` field to TenantInfo interface and fallback
- ✅ `config.toml` — added missing function entries (consume-token, renew-token, start-trial, etc.)
- ✅ Default Starble Master branding and 3 plans seeded

#### Verified Constraints:
- ✅ `device_id` used everywhere (not `hwid`)
- ✅ `chat` module disabled by default
- ✅ No tenant_id in community_messages or community_profiles
- ✅ Plans are fully dynamic (no hardcoded structure)
- ✅ Trial is once per user_id
