---
status: done
priority: medium
created: 2026-02-26
---

## Tarefa: Edge Function lovable-proxy recriada

### Descrição
Recriada do zero a edge function `lovable-proxy` como proxy direto para a API do Lovable usando o canal `security_fix_v2` (sem consumir créditos). A função aceita `task`, `lovable_token` e `project_id` no body, gera `id` e `ai_message_id` automaticamente, prefixa a mensagem com o anti-question prefix, e envia com `intent: "security_fix_v2"` + `chat_only: false` hardcoded. Suporte a `runtime_errors` opcional incluído.

### Resultado
- Função completamente reescrita (v4.0.0)
- CORS configurado
- Validação de inputs
- Parâmetros hardcoded conforme especificação
