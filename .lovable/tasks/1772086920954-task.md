---
status: done
priority: medium
created: 2026-02-26
---

## Tarefa: Recriar send-message e speed-chat como cópias do lovable-proxy

### Correção
Recriadas as Edge Functions `send-message` e `speed-chat` como cópias exatas do `lovable-proxy` v4.0.0, com compatibilidade para os formatos de body legados das extensões (`message`/`token`/`projectId` além de `task`/`lovable_token`/`project_id`).
