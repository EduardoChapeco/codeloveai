---
status: done
priority: high
created: 2026-02-26
completed: 2026-02-26
---

## Tarefa: Configurar Resend

### Problema
Resend não estava configurado — a edge function `send-email` referenciava tabela `api_keys` (inexistente) em vez de `api_key_vault`.

### Correção
- Secret `RESEND_API_KEY` adicionada como variável de ambiente
- `send-email` corrigida para usar tabela `api_key_vault` (nome correto)
- Adicionado fallback para `Deno.env.get("RESEND_API_KEY")` quando nenhuma chave existe no vault
- Corrigido contador de uso para usar campos corretos (`requests_count`, `last_used_at`)
