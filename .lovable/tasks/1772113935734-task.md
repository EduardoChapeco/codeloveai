---
status: done
priority: high
created: 2026-02-26
---

## Tarefa: Recriação de 5 Edge Functions

### Funções recriadas
1. `send-message` — Proxy para Lovable API com validação CLF1
2. `validate-hwid` — Validação de licença CLF1 + device info
3. `get-user-context` — Retorna branding, plano e links do usuário
4. `increment-usage` — Incrementa uso diário via DB function
5. `lovable-proxy` — Proxy genérico com suporte a x-clf-token header

### Correções aplicadas vs código original do prompt
- Corrigido `.eq("license_key", ...)` → `.eq("key", ...)` para compatibilidade com schema real
- Removidas referências a tabelas inexistentes (`usage_log`, `hwid_log`, `usage_daily`)
- `increment-usage` usa `increment_daily_usage` RPC existente no banco
- `get-user-context` mantém lógica completa com tenant/branding/plan/modules
