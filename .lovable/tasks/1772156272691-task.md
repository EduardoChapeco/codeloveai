---
status: done
priority: medium
created: 2026-02-27
completed: 2026-02-27
---

## Tarefa: Melhoria do Scraper/Miner de Respostas do Brain

### Implementado

1. **brain-capture-cron v4**: Reescrito com múltiplas estratégias de mineração:
   - `src/brain-output.md` (primário, requer frontmatter `status: done`)
   - `src/brain-output.json` (formato JSON legado)
   - `src/brain-output.html` (novo — extrai conteúdo de `<body>/<main>`)
   - `.lovable/tasks/*.md` (arquivos de tarefa com status done)
   - `latest-message` API (fallback final)

2. **Fingerprint de código-fonte**: Hash simples do source-code para skip de polls quando nada mudou, reduzindo chamadas desnecessárias à API.

3. **Agrupamento por usuário**: Token lookup único por usuário (não por conversa), reduzindo queries ao banco.

4. **Cron a cada 15 segundos**: Atualizado de 1min para 15s para captura quase em tempo real.

5. **Throughput aumentado**: Limite de 20 conversas por ciclo (era 10).

6. **Coluna `source_fingerprint`**: Adicionada à tabela `user_brain_projects` para persistir hash entre ciclos.
