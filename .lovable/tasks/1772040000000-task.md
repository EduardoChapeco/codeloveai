---
status: done
priority: high
created: 2026-02-25
completed: 2026-02-25
---

## Speed Extension — Edge Functions

### Implementado

1. **`validate-license`** — Atualizado com formato enriquecido:
   - `ok`, `valid`, `user { id, email, name }`, `plan { code, allowedExtensions, billingType, dailyMessages, usedToday, expiresAt }`
   - Rate limiting por HWID (10/min)
   - Audit log em `extension_audit_log`
   - Backward-compatible com campos legados

2. **`ext-load-branding`** — NOVO:
   - GET com header `X-Extension: speed`
   - Retorna branding, cores, links, módulos da tabela `whitelabel_config`
   - Cache 5min via `Cache-Control`

3. **`send-message`** — Já existia (v2.1.0), sem alterações necessárias

### DB criado
- `whitelabel_config` — config de branding por extensão/tenant
- `extension_audit_log` — log de auditoria de ações
