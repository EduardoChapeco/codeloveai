---
status: done
priority: medium
created: 2026-02-27
completed: 2026-02-27
---

## Tarefa: Melhoria da Mineração de .md do Brain

### Implementado

1. **Prompt reforçado**: `buildBrainPrompt` agora instrui explicitamente o Lovable a SEMPRE sobrescrever `src/brain-output.md` com frontmatter `status: done`, além de gerar `src/brain-output.json` como backup.

2. **Captura multi-estratégia aprimorada**: `captureResponse` agora verifica:
   - `src/brain-output.md` (primário)
   - `src/brain-output.json` (secundário)
   - `src/brain-output.html` (terciário)
   - `.lovable/tasks/*.md` com status done
   - `latest-message` API (fallback)

3. **Polling mais rápido**: Intervalo reduzido de 4s para 3s, delay inicial de 6s para 5s.

4. **Realtime no frontend**: Adicionado polling de 3s no frontend que verifica diretamente a tabela `loveai_conversations` para detectar quando o `brain-capture-cron` captura a resposta, mostrando ao cliente imediatamente.

5. **Todas as skills cobertas**: Adicionadas instruções de output para skills `data`, `devops` e `security` que estavam faltando.
