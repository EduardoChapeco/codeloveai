---
status: done
priority: high
created: 2026-03-02
---

## Tarefa: CORREÇÕES (fIX) cIRIUS

### Resultado da execução
- Runtime error crítico corrigido.
- Pipeline `build_prompt` implementado no backend para iniciar geração real.
- Editor conectado a logs reais para bolhas de tarefa em tempo real.
- Preview conectado a campos reais (`lovable_project_id`/`brain_project_id` + fallback `source_files_json`).
- Domínio e SEO agora persistem no banco (domínio em `custom_domain`, SEO em `deploy_config.seo`).
- Fluxo estabilizado para não depender de mock/simulação no envio principal.
