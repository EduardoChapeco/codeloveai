---
status: done
priority: high
created: 2026-02-26
---

## Tarefa: Implementar Ghost Create no Brain

### Descrição
O fluxo de criação do Brain foi refatorado para usar o padrão Ghost Create corretamente.

### Correções Aplicadas
1. **Ghost Create com payload correto**: A criação usa `initial_message` como objeto com `id`, `message`, `files`, `ai_message_id` — formato exigido pela API (payload string causava 400 malformed_request)
2. **Cancel imediato**: O `msgId` gerado localmente é usado para cancel via `POST /projects/{id}/chat/{msgId}/cancel` para custo zero
3. **Injeção de config pós-cancel**: Arquivos `brain-config.md` e `brain-output.json` injetados via `edit-code`
4. **Prompts reforçados**: Instrução explícita de não criar componentes e escrever apenas em `brain-output.json`
5. **Captura com fallback MD**: `captureResponse` verifica `brain-output.json` e `.lovable/tasks/brain-response.md`
6. **Lock anti-stale**: Locks expiram após 2 minutos

### Arquivos Alterados
- `supabase/functions/loveai-brain/brain-helpers.ts` — Ghost Create + captura melhorada
