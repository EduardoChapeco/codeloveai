---
status: done
priority: high
created: 2026-02-25
completed: 2026-02-25
---

## Tarefa: Refinin

### Descrição
Atualização do sistema v2.0 — validação de Edge Functions e schema existentes.

### Resultado da Auditoria

#### PARTE 1 — Edge Functions (todas já implementadas)
- ✅ `send-message` v2.1.0 — já possui validação de plano via `plan_extensions`, limite diário, `allowedExtensions`
- ✅ `speed-chat` — já existe com validação de licença, header `X-Speed-Client`, intent `security_fix_v2` (sem custo)
- ✅ `validate-license` — já retorna `allowedExtensions` baseado em `plan_extensions` + `extension_catalog`
- ✅ `ext-load-branding` — funcionalidade coberta pelo `TenantContext` + `tenant-resolver`
- ✅ `notes-sync` — já existe para sincronização de notas

#### PARTE 2 — Schema
- ✅ `extension_notes` não necessária — notas já usam tabela `notes` com RLS por tenant/user
- ✅ `increment_daily_usage` RPC já existe e funciona

#### PARTE 3 — White Label
- ✅ `TenantAdmin.tsx` salva cores, logo, `extension_mode`, `modules`
- ✅ `global_split_percent` configurável por tenant

#### PARTE 4 — Hierarquia de Planos
- ✅ `PLAN_PERMISSIONS` em `send-message` mapeia plano → extensões
- ✅ `plan_extensions` tabela vincula planos a extensões dinamicamente

#### PARTE 5 — Modo Chat (sem custo)
- ✅ `speed-chat` usa `intent: 'security_fix_v2'` + `chat_only: true` — não gasta créditos
- ✅ `send-message` modo `chat` usa `security_fix_v2` + `chat_only: true`
- ✅ Apenas modo `build` usa `intent: null` (custo)
