---
status: done
priority: high
created: 2026-03-02
completed: 2026-03-02
---

## Tarefa: Executar task de refinamento, correções

### Implementado (real)

1. **CiriusNew → Editor redirect** — Navegação pós-criação agora vai para `/cirius/editor/:id`
2. **lovable_project_id propagation** — Já implementado em `cirius-generate` action `generate_code` (linhas 932-938)
3. **md-assembly → cirius sync** — Já implementado em `orchestrator-tick` via `syncLatestMarkdownFiles`
4. **Auto-refinement trigger** — Já implementado em `orchestrator-tick` via `autoCapture` → `triggerRefinement`
5. **FIX: Refine auth bug** — `refine` action agora aceita service-key calls (antes falhava silenciosamente com 401 quando chamado pelo orchestrator-tick)
6. **FIX: Auto-deploy após refinamento** — Após refine bem-sucedido, sistema dispara auto-deploy GitHub se integração ativa
7. **Chat persistence** — Criada tabela `cirius_chat_messages` com RLS, frontend carrega/salva mensagens entre sessões

### Bugs corrigidos
- `triggerRefinement` chamava `cirius-generate` com service key, mas o handler exigia `user` auth → refinamento nunca executava
- Chat perdia histórico ao recarregar página
