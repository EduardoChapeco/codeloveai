---
status: done
priority: high
created: 2026-03-02
completed: 2026-03-02
---

## Tarefa: Executar task de refinamento, correções

### Implementado (real) — Auditoria Completa

| # | Correção | Status | Evidência |
|---|---------|--------|-----------|
| 1 | CiriusNew → `/cirius/editor/:id` | ✅ | `src/pages/CiriusNew.tsx` L119: `navigate(\`/cirius/editor/${projectId}\`)` |
| 2 | `lovable_project_id` propagação | ✅ | `cirius-generate` L935-941: salva `lovable_project_id`, `brain_project_id`, `preview_url` |
| 3 | md-assembly → source_files_json | ✅ | `orchestrator-tick` L92-174: `syncLatestMarkdownFiles()` chamada L417-424 após cada task |
| 4 | Auto-refine trigger | ✅ | `orchestrator-tick` L200: `triggerRefinement()` chamada em `autoCapture()` L560 |
| 5 | Auto-deploy após refine | ✅ | `cirius-generate` L1281-1324: status→deploying, dispara cirius-deploy |
| 6 | cirius-deploy marca live | ✅ | `cirius-deploy` L141-143: `status:"live"`, `progress_pct:100`, `current_step:"done"`, `generation_ended_at` |
| 7 | cirius_chat_messages + persistência | ✅ | Tabela existe, `CiriusEditor.tsx` L134-152 carrega, L234-243 persiste |
| 8 | Preview Realtime | ✅ | `CiriusEditor.tsx` L155-189: subscription para UPDATE em cirius_projects |

### Bugs corrigidos nesta sessão

1. **CRÍTICO: cirius-deploy 401 em auto-deploy** — `cirius-deploy` exigia `user` auth, mas auto-deploy de `refine` usava service key → deploy nunca executava. Adicionado `isServiceKey` check + `projectUserId` para lookup de integrações.
2. **cirius-deploy não finalizava status** — GitHub deploy não setava `progress_pct: 100`, `generation_ended_at`, `current_step: "done"`. Corrigido.
3. **Faltava Vercel chain deploy** — Após GitHub push, sistema agora verifica integração Vercel ativa e dispara deploy automático em cadeia.
4. **refine não atualizava status antes de deploy** — Adicionado `status: "deploying"`, `current_step: "deploy_github"`, `progress_pct: 90` antes de disparar cirius-deploy.
5. **Sem GitHub = projeto ficava em limbo** — Se não há integração GitHub, refine agora marca projeto como `live` diretamente com `progress_pct: 100`.

### Checklist Final

- [x] 1. CiriusNew.tsx: navigate aponta para /cirius/editor/:id
- [x] 2. cirius_projects.lovable_project_id salvo após generate_code
- [x] 3. syncLatestMarkdownFiles chamada após cada task concluída
- [x] 4. autoCapture → triggerRefinement dispara cirius-generate action:refine
- [x] 5. action:refine dispara cirius-deploy action:github automaticamente
- [x] 6. cirius_projects.status = 'live' após deploy GitHub
- [x] 7. Tabela cirius_chat_messages existe com RLS + Realtime
- [x] 8. Chat carrega histórico ao abrir o editor
- [x] 9. Preview recarrega via Realtime quando source_files_json muda
- [x] 10. Nenhum navigate para /cirius/project/:id no fluxo de criação
